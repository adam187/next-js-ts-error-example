{
  "scripts": {
    "dev": "nodemon",
    "build": "next build && tsc && tsc --project tsconfig.server.json",
    "start": "cross-env NODE_ENV=production node .next/server",
    "ts:check": "tsc -p tsconfig.json --noEmit",
    "ts:check:server": "tsc -p tsconfig.server.json --noEmit"
  },
  "dependencies": {
    "next": "^8.1.1-canary.24",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "@types/next": "^8.0.5",
    "@types/react": "^16.8.8",
    "cross-env": "^5.2.0",
    "nodemon": "^1.18.10",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.5"
  }
}
